import { GameMode } from '../core/modes'
import { Keys } from '../core/interactions'

class EditMode<TComponents> extends GameMode<TComponents> {
  start = () => {

  }

  tick = (elapsedTime: number) => {

  }
  
  stop = () => {

  }
}